{
  "name": "Python UV + CLI DevPod",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "postCreateCommand": "nvim --version && uv --version && opencode --version && lazygit --version && act --version && docker --version && docker compose version && git config --local --add safe.directory /workspaces/devpods-builds && git remote -v",
  "remoteUser": "vscode"
}
