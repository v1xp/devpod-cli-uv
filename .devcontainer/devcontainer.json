{
  "name": "Python UV + CLI DevPod",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "nvim --version && uv --version && opencode --version && lazygit --version && act --version && git config --local --add safe.directory /workspaces/devpods-builds && git remote -v",
  "remoteUser": "vscode"
}
